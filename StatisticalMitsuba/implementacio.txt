def render()
  for spp:
    sample()


def sample()
  for rebote:
    add_transient_data(Le)
    add_transient_data(Li)



ImageBlock

sample_tensor = shape(y, x, spp) values(sumas de los rebotes de cada sample)
transient_actual_bin = shape(y, x, spp) values(transient bin actual)

al cambiar el transient_actual_bin hay que hacer bc cuadrado y cubo y añadirlo a los tensores de sum1, sum2, sum3



